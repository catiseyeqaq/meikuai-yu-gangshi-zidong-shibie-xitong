# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Simplified CI for Automatic-Identification-of-Coal-Mines-and-Ores

name: CI

permissions:
  contents: read

env:
  PYTHONFAULTHANDLER: 1

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  Tests:
    timeout-minutes: 60
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ["3.12"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python }}
          activate-environment: ${{ matrix.os == 'cpu-latest' }}
      - name: Install requirements
        shell: bash
        run: |
          uv pip install ${{ matrix.os != 'cpu-latest' && '--system' || '' }} -e ".[dev]" --extra-index-url https://download.pytorch.org/whl/cpu --index-strategy unsafe-best-match
      - name: Check environment
        run: |
          yolo checks
          uv pip list
      - name: Pytest tests
        shell: bash
        run: pytest --cov=ultralytics/ --cov-report=xml tests/
